<% @followed_stocks.each do |follow, stock| %>
  $(".customs .forstocks tbody").append('<%= j render partial: "profiles/profile/stock_item_#{@render_type}", locals: {stock: stock, follow: follow} %>');
<% end %>