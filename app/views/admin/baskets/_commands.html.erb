<% if basket.recommend? %>
  <%= link_to raw("<i class='glyphicon glyphicon-hand-down'></i>解除推荐"), discommend_admin_basket_path(basket), remote: true  %>  
<% else %>
  <%= link_to raw("<i class='glyphicon glyphicon-hand-up'></i>推荐"), recommend_admin_basket_path(basket), remote: true  %>
<% end %>
