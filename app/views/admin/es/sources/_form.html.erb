<%= form_for [:admin, @es_source] do |f| -%>
  <table class="grid_form">
    <tbody>
      <tr>
        <td class="field"><b></b><%= f.label :code %></td>
        <td><%= f.text_field :code, size: 30 %></td>
      </tr>
      <tr>
        <td class="field"><b></b><%= f.label :name %></td>
        <td><%= f.text_field :name, size: 30 %></td>
      </tr>
      <tr>
        <td class="field"><b></b><%= f.label :url %></td>
        <td><%= f.text_field :url, size: 30 %></td>
      </tr>
      <tr>
        <td class="field"></td>
        <td><%= f.submit '提交' %></td>
      </tr>
    </tbody>
  </table>
  
<% end -%>


