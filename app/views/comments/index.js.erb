$(".comments li").remove();

<% @comments.each do |comment| %>
  $(".comments").append("<%= j render partial: '/comments/item', locals: {comment: comment} %>");
<% end %>

<% if @comments.total_pages > 1 %>
  $(".comments").siblings(".pageNav").remove();
  $(".comments").after("<%= j render partial: '/comments/paginate', locals: {comments: @comments} %>");
  Comment.convertCommentPaginateToAjaxLink();
<% end %>

initBubbleBox();
