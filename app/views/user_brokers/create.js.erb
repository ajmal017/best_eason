<% if @result %>
  window.location.href = '/user_brokers/succ'
<% else %>
  <% msg = @user_broker.errors.messages %>
  $('.errorTip').text("<%= msg[:broker_id].first || msg[:broker_no].first %>").show();
<% end %>
