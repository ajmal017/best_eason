#auto_required_paths = %W(app/consumers/utils app/consumers app/consumers/errors app/consumers/handlers)

## 自动加载目录
#auto_required_paths.each do |d|
  #Dir["#{Rails.root}/#{d}/*.rb"].each {|p| require p}
#end
